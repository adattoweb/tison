import { Types } from "@/features/types/Types"
import { PackagePlus } from "lucide-react"

export const types = {
   path: "types",
   Component: Types,
   handle: {
      label: "Типи продуктів",
      Icon: PackagePlus,
      nav: true,
   },
}
