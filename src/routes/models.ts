import { Models } from "@/features/types/Models"
import { PackagePlus } from "lucide-react"

export const models = {
   path: "models",
   Component: Models,
   handle: {
      label: "Моделі продуктів",
      Icon: PackagePlus,
      nav: true,
   },
}
